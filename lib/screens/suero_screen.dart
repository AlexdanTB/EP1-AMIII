import 'package:flutter/material.dart';

class SueroScreen extends StatelessWidget {
  const SueroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Suero'));
  }
}
